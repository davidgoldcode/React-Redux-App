
const initialState = {
    recipes: [], 
    loadingTacos: true,
    errorMessage: '',
}

export default (state = initialState, action) => {
    switch(action.type) {
        default: 
            return state;
    }
}